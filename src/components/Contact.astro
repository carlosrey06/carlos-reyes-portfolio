<section id="contact" class="contact" aria-labelledby="contact-title">
	<div class="contact__inner">
		<header class="contact__header">
			<p class="contact__eyebrow">05 / CONTACTO</p>
			<h2 id="contact-title">
				<span>¿Tienes una idea</span>
				<span>o un proyecto?</span>
			</h2>
			<p class="contact__description">
				Si quieres hablar de un proyecto, colaboración u oportunidad, puedes contactarme por estos medios.
			</p>
		</header>

		<div class="contact__channels" aria-label="Canales de contacto">
			<a
				class="contact__channel"
				href="https://github.com/carlosrey06"
				target="_blank"
				rel="noopener noreferrer"
				aria-label="Visitar el perfil de GitHub de Carlos Reyes (se abre en una pestaña nueva)"
			>
				<span class="contact__channel-label">GitHub</span>
				<span class="contact__channel-value">github.com/carlosrey06</span>
				<span class="contact__channel-arrow" aria-hidden="true">↗</span>
			</a>

			<a
				class="contact__channel"
				href="https://www.linkedin.com/in/carlos-reyes-03b95b2aa"
				target="_blank"
				rel="noopener noreferrer"
				aria-label="Visitar el perfil de LinkedIn de Carlos Reyes (se abre en una pestaña nueva)"
			>
				<span class="contact__channel-label">LinkedIn</span>
				<span class="contact__channel-value">Carlos Reyes</span>
				<span class="contact__channel-arrow" aria-hidden="true">↗</span>
				<span class="contact__channel-secondary">linkedin.com/in/carlos-reyes-03b95b2aa</span>
			</a>

			<a
				class="contact__channel"
				href="tel:9934263748"
				aria-label="Llamar a Carlos Reyes al 993 426 3748"
			>
				<span class="contact__channel-label">Teléfono</span>
				<span class="contact__channel-value">993 426 3748</span>
				<span class="contact__channel-arrow" aria-hidden="true">→</span>
			</a>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: 0 0 clamp(104px, 18vw, 152px);
	}

	.contact__inner {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		row-gap: 56px;
		column-gap: 16px;
		width: min(100%, var(--content-width));
		margin: 0 auto;
		padding-inline: var(--page-padding);
	}

	.contact__header,
	.contact__channels {
		grid-column: 1 / -1;
	}

	.contact__header {
		max-width: 48rem;
	}

	.contact__eyebrow {
		margin: 0 0 18px;
		color: var(--color-cyan);
		font-family: var(--font-ui);
		font-size: 0.72rem;
		font-weight: 600;
		letter-spacing: 0.12em;
		line-height: 1.4;
		text-transform: uppercase;
	}

	.contact h2 {
		margin: 0;
		color: var(--color-text);
		font-family: var(--font-display);
		font-size: clamp(1.85rem, 8.5vw, 3rem);
		font-weight: 600;
		letter-spacing: 0.02em;
		line-height: 1;
		text-transform: uppercase;
	}

	.contact h2 span {
		display: block;
	}

	.contact__description {
		max-width: 37rem;
		margin: 28px 0 0;
		color: var(--color-muted);
		font-family: var(--font-ui);
		font-size: clamp(1rem, 2.8vw, 1.08rem);
		line-height: 1.7;
	}

	.contact__channels {
		border-bottom: 1px solid var(--color-border);
	}

	.contact__channel {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		gap: 7px 18px;
		align-items: center;
		min-height: 88px;
		padding-block: 18px;
		border-top: 1px solid var(--color-border);
		color: var(--color-text);
		font-family: var(--font-ui);
		text-decoration: none;
	}

	.contact__channel-label {
		grid-column: 1 / -1;
		color: var(--color-muted);
		font-size: 0.72rem;
		font-weight: 600;
		letter-spacing: 0.11em;
		line-height: 1.4;
		text-transform: uppercase;
	}

	.contact__channel-value {
		min-width: 0;
		font-size: clamp(1rem, 4.6vw, 1.12rem);
		font-weight: 500;
		letter-spacing: 0.015em;
		line-height: 1.45;
		overflow-wrap: anywhere;
	}

	.contact__channel-arrow {
		color: var(--color-cyan);
		font-size: 0.9rem;
		line-height: 1;
		transition: transform 180ms ease;
	}

	.contact__channel-secondary {
		grid-column: 1 / -1;
		min-width: 0;
		color: var(--color-muted);
		font-size: 0.82rem;
		letter-spacing: 0.015em;
		line-height: 1.45;
		overflow-wrap: anywhere;
	}

	.contact__channel:focus-visible {
		border-radius: 1px;
	}

	@media (hover: hover) and (pointer: fine) {
		.contact__channel:hover .contact__channel-value {
			color: var(--color-cyan);
		}

		.contact__channel:hover .contact__channel-arrow {
			transform: translate(2px, -2px);
		}
	}

	@media (prefers-reduced-motion: no-preference) {
		.contact__header,
		.contact__channels {
			animation: contact-reveal 700ms ease-out both;
		}
	}

	@keyframes contact-reveal {
		from {
			opacity: 0;
			transform: translateY(8px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (min-width: 768px) {
		.contact {
			padding-bottom: clamp(128px, 14vw, 176px);
		}

		.contact__inner {
			grid-template-columns: repeat(8, minmax(0, 1fr));
			column-gap: 20px;
		}

		.contact__header {
			grid-column: 1 / span 7;
		}

		.contact__channels {
			grid-column: 2 / span 7;
		}

		.contact h2 {
			font-size: clamp(3rem, 6.5vw, 4.25rem);
		}
	}

	@media (min-width: 1024px) {
		.contact__inner {
			grid-template-columns: repeat(12, minmax(0, 1fr));
			column-gap: clamp(24px, 2vw, 32px);
		}

		.contact__header {
			grid-column: 1 / span 7;
			grid-row: 1;
		}

		.contact__channels {
			grid-column: 9 / span 4;
			grid-row: 1;
			align-self: end;
		}

		.contact h2 {
			font-size: clamp(3rem, 5vw, 4.5rem);
		}
	}
</style>
