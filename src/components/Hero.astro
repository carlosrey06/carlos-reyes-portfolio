<section class="hero" aria-labelledby="hero-name">
	<div class="hero__atmosphere" aria-hidden="true"></div>

	<div class="hero__inner">
		<p class="hero__title">
			<span>FULL STACK DEVELOPER</span>
			<span>&amp; SYSTEMS ENGINEER</span>
		</p>

		<h1 id="hero-name" class="hero__name">
			<span>CARLOS</span>
			<span>REYES</span>
		</h1>

		<p class="hero__description">
			Desarrollo aplicaciones web completas y la infraestructura necesaria para llevarlas a producción de
			forma segura y mantenible.
		</p>

		<div class="hero__actions" aria-label="Acciones principales">
			<!-- Pending wiring: the projects destination does not exist yet. -->
			<button class="hero__cta hero__cta--primary" type="button" disabled data-pending-cta="projects">
				Ver proyectos
			</button>

			<!-- Pending wiring: the final CV asset does not exist in public/ yet. -->
			<button class="hero__cta hero__cta--secondary" type="button" disabled data-pending-cta="cv">
				Descargar CV
			</button>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		isolation: isolate;
		display: flex;
		min-height: max(560px, min(calc(100svh - 48px), 900px));
		overflow: hidden;
		align-items: stretch;
	}

	.hero::before {
		position: absolute;
		z-index: -2;
		top: 18%;
		right: -48vw;
		width: 120vw;
		height: 120vw;
		border: 1px solid color-mix(in srgb, var(--color-cyan) 18%, transparent);
		border-radius: 50%;
		content: '';
		opacity: 0.7;
		transform: rotate(-12deg);
	}

	.hero::after {
		position: absolute;
		z-index: -1;
		top: 28%;
		left: var(--page-padding);
		width: min(38vw, 240px);
		height: 1px;
		background: linear-gradient(90deg, var(--color-border), transparent);
		content: '';
		opacity: 0.8;
	}

	.hero__atmosphere {
		position: absolute;
		z-index: -1;
		display: none;
		right: 13%;
		bottom: 20%;
		width: 22px;
		height: 22px;
		border: 1px solid color-mix(in srgb, var(--color-gold) 42%, transparent);
		transform: rotate(45deg);
	}

	.hero__inner {
		box-sizing: border-box;
		display: flex;
		flex-direction: column;
		justify-content: center;
		width: min(100%, var(--content-width));
		margin: 0 auto;
		padding: 48px var(--page-padding) 64px;
	}

	.hero__title {
		max-width: 19rem;
		margin: 0 0 28px;
		color: var(--color-muted);
		font-family: var(--font-ui);
		font-size: 0.76rem;
		font-weight: 600;
		letter-spacing: 0.1em;
		line-height: 1.45;
		text-transform: uppercase;
		animation: hero-rise 500ms ease-out 100ms both;
	}

	.hero__title span {
		display: block;
	}

	.hero__name {
		display: flex;
		flex-direction: column;
		gap: 0;
		max-width: 100%;
		margin: 0;
		color: var(--color-text);
		font-family: var(--font-display);
		font-size: clamp(3.4rem, 17vw, 6rem);
		font-weight: 700;
		letter-spacing: 0.045em;
		line-height: 0.93;
		text-wrap: balance;
		animation: hero-name-reveal 1100ms cubic-bezier(0.22, 1, 0.36, 1) both;
	}

	.hero__name span {
		display: block;
	}

	.hero__description {
		max-width: 36rem;
		margin: 32px 0 0;
		color: var(--color-muted);
		font-family: var(--font-ui);
		font-size: clamp(1rem, 2.8vw, 1.125rem);
		line-height: 1.65;
		animation: hero-rise 650ms ease-out 250ms both;
	}

	.hero__actions {
		display: flex;
		flex-direction: column;
		align-items: stretch;
		gap: 12px;
		max-width: 22rem;
		margin-top: 36px;
		animation: hero-rise 650ms ease-out 350ms both;
	}

	.hero__cta {
		min-height: 52px;
		padding: 14px 20px;
		border: 1px solid transparent;
		border-radius: 8px;
		font-family: var(--font-ui);
		font-size: 0.9375rem;
		font-weight: 600;
		line-height: 1.2;
		text-align: center;
		transition:
			background-color 180ms ease,
			border-color 180ms ease,
			color 180ms ease;
	}

	.hero__cta:disabled {
		cursor: default;
		opacity: 1;
	}

	.hero__cta--primary {
		background: var(--color-blue);
		color: var(--color-text);
	}

	.hero__cta--secondary {
		align-self: center;
		min-height: 48px;
		padding-inline: 12px;
		border: 0;
		border-bottom: 1px solid var(--color-border-strong);
		border-radius: 0;
		background: transparent;
		color: var(--color-text);
	}

	@keyframes hero-name-reveal {
		from {
			opacity: 0;
			letter-spacing: 0.13em;
			transform: translateY(12px);
		}

		to {
			opacity: 1;
			letter-spacing: 0.045em;
			transform: translateY(0);
		}
	}

	@keyframes hero-rise {
		from {
			opacity: 0;
			transform: translateY(8px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (min-width: 640px) and (max-width: 767px) {
		.hero__inner {
			padding-top: 56px;
		}

		.hero__name {
			font-size: clamp(4.2rem, 13vw, 5.8rem);
		}
	}

	@media (min-width: 768px) and (max-width: 1023px) {
		.hero {
			min-height: max(640px, min(calc(100svh - 48px), 860px));
		}

		.hero::before {
			top: -8vw;
			right: -30vw;
			width: 86vw;
			height: 86vw;
		}

		.hero__atmosphere {
			display: none;
		}

		.hero__inner {
			display: grid;
			grid-template-columns: repeat(8, minmax(0, 1fr));
			column-gap: 20px;
			align-content: center;
			padding-top: 64px;
			padding-bottom: 72px;
		}

		.hero__title {
			grid-column: 1 / span 6;
			max-width: 36rem;
		}

		.hero__name,
		.hero__description,
		.hero__actions {
			grid-column: 1 / span 6;
		}

		.hero__name {
			font-size: clamp(6.25rem, 12vw, 8.75rem);
			letter-spacing: 0.055em;
		}

		.hero__name span {
			display: block;
		}

		.hero__actions {
			flex-direction: row;
			align-items: center;
			max-width: none;
		}

		.hero__description {
			max-width: 34rem;
		}

		.hero__cta {
			min-width: 10rem;
		}
	}

	@media (min-width: 1024px) {
		.hero {
			min-height: min(100svh, 900px);
		}

		.hero::before {
			top: -18vw;
			right: -18vw;
			width: 72vw;
			height: 72vw;
		}

		.hero__inner {
			display: grid;
			grid-template-columns: repeat(12, minmax(0, 1fr));
			column-gap: clamp(24px, 2vw, 32px);
			align-content: center;
			padding-top: 96px;
			padding-bottom: 128px;
		}

		.hero__title,
		.hero__name,
		.hero__description,
		.hero__actions {
			grid-column: 1 / span 7;
		}

		.hero__atmosphere {
			display: none;
		}

		.hero__title {
			max-width: none;
			font-size: 0.82rem;
		}

		.hero__name {
			font-size: clamp(5rem, 8vw, 9rem);
			letter-spacing: 0.055em;
		}

		.hero__name span {
			display: block;
		}

		.hero__actions {
			flex-direction: row;
			align-items: center;
			max-width: none;
		}

		.hero__description {
			max-width: 42rem;
		}

		.hero__cta {
			min-width: 10rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.hero__title,
		.hero__name,
		.hero__description,
		.hero__actions {
			animation: none;
		}
	}
</style>
